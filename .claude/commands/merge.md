---
description: 現在のブランチのPRをマージし、mainに戻って最新化する（マージ不可なら理由を報告して停止）
---

現在のブランチのPRをマージしてください:

1. `gh pr view` で現在のPRの状態を確認
2. PRが存在すれば `gh pr merge --merge --delete-branch` でマージ
3. main ブランチに切り替え
4. `git pull` で最新を取得
5. 完了を報告

もしマージできない状態（レビュー待ち、CI失敗など）であれば、その理由を報告して停止してください。
